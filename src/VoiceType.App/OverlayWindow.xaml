<Window x:Class="VoiceType.App.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VoiceType"
        Width="460" Height="76"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ShowActivated="False"
        Focusable="False" ResizeMode="NoResize" IsHitTestVisible="False">
    <Border CornerRadius="12" Background="#E6202124" Padding="16,8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Ellipse x:Name="StatusDot" Width="10" Height="10" Fill="#D93025" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock x:Name="StatusText" Foreground="White" FontSize="13" VerticalAlignment="Center"
                           Text="Recording"/>
                <Border Width="80" Height="6" Background="#3C4043" CornerRadius="3" Margin="12,0,0,0" VerticalAlignment="Center">
                    <Rectangle x:Name="LevelBar" Height="6" Width="0" Fill="#34A853" RadiusX="3" RadiusY="3" HorizontalAlignment="Left"/>
                </Border>
            </StackPanel>

            <TextBlock x:Name="PartialText" Grid.Row="1" Foreground="#BDC1C6" FontSize="12"
                       Margin="20,6,0,0" TextTrimming="CharacterEllipsis" MaxWidth="410"
                       HorizontalAlignment="Left" Text=""/>
        </Grid>
    </Border>
</Window>
